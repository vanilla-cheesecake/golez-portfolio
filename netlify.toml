[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.18.0"

# vue-router uses createWebHistory, so deep links must fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
